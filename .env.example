# EntraID (Azure AD) Authentication Configuration
# Copy this file to .env and fill in your values

# Required: Your EntraID Application (Client) ID
# Get this from Azure Portal > App registrations > Your App > Overview
VITE_ENTRA_CLIENT_ID=your-client-id-here

# Optional: Tenant ID (defaults to 'common' for multi-tenant apps)
# Use 'common' for multi-tenant, 'organizations' for work/school accounts only,
# 'consumers' for personal Microsoft accounts only, or your specific tenant ID
VITE_ENTRA_TENANT_ID=common

# Optional: Redirect URI (defaults to current origin)
# Must match what's configured in Azure Portal
VITE_ENTRA_REDIRECT_URI=http://localhost:5173

# Optional: Scopes (defaults to 'openid profile email User.Read')
# Space-separated list of OAuth scopes to request
VITE_ENTRA_SCOPES=openid profile email User.Read
